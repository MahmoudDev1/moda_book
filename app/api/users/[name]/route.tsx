// import { searchByName } from "@/lib/user";
// import { NextRequest, NextResponse } from "next/server";

// export async function GET(req: NextRequest, { params }: { params: { name: string } }, res: NextResponse) {
//   const users = await searchByName(params.name)
//   console.log(users);

//   return Response.json({ test: "test" });
// }
